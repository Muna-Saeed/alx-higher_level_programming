// Toggle between 'red' and 'green' classes on the <header> element
// when user clicks the div#toggle_header tag

$('div#toggle_header').click(function () {
    $('header').toggleClass('red  green');
  });
